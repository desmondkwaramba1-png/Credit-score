// Sidebar.jsx — kept as minimal stub for backward compatibility.
// The app now uses a top navigation layout via Shell.jsx + TopNav.jsx.
// This file is intentionally empty; the useSidebar() context is provided by Shell.jsx.

export function Sidebar() {
  return null;
}
